# references/

This directory is **immutable reference data**. Treat everything here as read-only.

- Do not modify files in this directory.
- Do not import directly from this directory in `src/`.
- Any code, data, or logic you want to use should be copied or re-implemented in `src/`.

## Contents

| Directory | Description |
| --- | --- |
| `KELE/` | Original KELE baseline — translated source, dataset, and attribution (see [`KELE/ATTRIBUTION.md`](KELE/ATTRIBUTION.md)) |
| `requirements/` | Course guidelines |
