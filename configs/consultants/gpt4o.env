# GPT-4o via OpenAI API (baseline experiment, matching paper's original setup).
# CONSULTANT_API_KEY must be set in .env (do NOT commit it here).
CONSULTANT_BASE_URL=https://api.openai.com/v1
CONSULTANT_MODEL_NAME=gpt-4o-2024-11-20
CONSULTANT_MAX_TOKENS=4096
CONSULTANT_DISABLE_THINKING=false
