# Experiment: Baseline (reproducing paper)
# Teacher: SocratTeachLLM via vLLM
# Consultant: GPT-4o via OpenAI API (matches paper's original setup)
# CONSULTANT_API_KEY must be set in .env (do NOT commit the OpenAI key).
source configs/teachers/socrat-vllm.env
source configs/consultants/gpt4o.env
source configs/common.env
